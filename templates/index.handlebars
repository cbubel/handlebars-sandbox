{{> partials/header}}

<div id="main" class="container">
  {{#if page.isHome}}
    {{> partials/home}}
  {{else if page.isAbout}}
    {{> partials/about}}
  {{else if page.isContact}}
    {{> partials/contact}}
  {{else}}
    {{> partials/notFound}}
  {{/if}}
</div>

{{> partials/footer}}